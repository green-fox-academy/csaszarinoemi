class Garden {
  flower: number;
  tree: number;

  constructor(flower: number, tree: number) {
    this.flower = flower;
    this.tree = tree;
  }

  watering(element){
    if (element.waterlevel < 5) {

    }
  }
}